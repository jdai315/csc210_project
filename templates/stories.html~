{% extends 'base.html' %}

{% block content %}



{% set active_page = "findStory" %}

{% set navigation_bar = [
('/addstory', 'addStory', 'Add Story'),
('/stories', 'findStory', 'Find a Story'),
('/logout', 'logout', 'Logout'),
] -%}


<div class="header">
  <div class="home-menu pure-menu pure-menu-horizontal pure-menu-fixed">
    <a class="pure-menu-heading" href="/addstory">Novel</a>
    
    <ul class="pure-menu-list">
      {% for href, id, caption in navigation_bar %}
      <li class="pure-menu-item {% if id == active_page %}pure-menu-selected{% endif %}"><a href="{{ href|e }}" class="pure-menu-link">{{ caption|e }}</a></li>
      {% endfor %}
    </ul>
  </div>
</div>

<div class="content">
  
  <div class=" l-box-lrg pure-g">
    <div class="pure-u-1">
      
      <h1 class="content-head is-center">Welcome {{ user }}.</h1>

      {% with msgs = get_flashed_messages() %}
      {% if msgs %}
      {%- for msg in msgs %}
      <div class="alert alert-info splash-subhead" id="flashed">
	{{ msg }}
      </div>
      {% endfor -%}
      {% endif %}
      {% endwith %}
      
      <div class="pure-g">
	<div class="l-box-lrg pure-u-1 pure-u-md-2-5">	  
	 
	  <form method="POST" class="pure-form pure-form-stacked">
            <legend><div class="content-subhead">Find a story by title</div></legend>
            
	    <fieldset>
              <label for"findTitle">Search by Title</label>
	      <input name="findTitle" type="text" class="pure-input-1" placeholder="Story Title">
              
              <button type="submit" class="pure-button pure-input-1-3 pure-button-primary is-center">Submit</button>
            </fieldset>
	    
          </form>
        
	</div>

	<div class="l-box-lrg pure-u-1 pure-u-md-2-5">	  
	  <h3 class="content-subhead">{{ loadTitle|safe }}</h3>
	  <div id="story">
            <p class="break"> {{ loadContent|safe }} </p>
          </div>
	</div>
	
      </div> <!-- end of l-box-->
    </div>
    
  </div>
</div> <!-- end of content-->

{% endblock %}
